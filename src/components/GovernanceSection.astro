---
const levels = [
  ["岗位职责", "明确 AI 员工负责什么、不负责什么，以及由谁承担最终责任。"],
  ["SOP 与能力", "规定可执行流程、可调用工具、可访问数据和完成标准。"],
  ["协作与升级", "允许 Agent 向人类发布任务，在关键节点请求判断和审批。"],
  ["审计与追责", "记录任务来源、执行过程、决策依据、产物和责任归属。"]
];
---

<section class="governance-section section" id="governance">
  <div class="container governance-grid">
    <div class="governance-copy" data-reveal>
      <h2>自主始终服从岗位和责任边界。</h2>
      <p>SilicoEco 把能力、权限和责任分开管理。Agent 知道如何行动，不代表它已经获得行动许可。</p>
    </div>
    <div class="autonomy-ladder" data-reveal>
      {levels.map(([title, body], index) => (
        <article style={`--level:${index}`}>
          <span></span>
          <div><strong>{title}</strong><p>{body}</p></div>
        </article>
      ))}
    </div>
  </div>
</section>
