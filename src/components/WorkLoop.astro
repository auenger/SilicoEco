---
const steps = [
  ["意图", "明确希望发生的改变"],
  ["工作", "形成可管理、可验收的 Task"],
  ["上下文", "加载完成工作真正需要的信息"],
  ["执行", "交给合适的人、Agent 或流程"],
  ["判断", "用证据完成审批、验收与调整"],
  ["知识", "把结果写回组织，进入下一次工作"]
];
---

<section class="work-loop section" id="loop">
  <div class="container">
    <div class="section-copy compact-copy" data-reveal>
      <h2>工作不是一次对话，而是一条持续推进的闭环。</h2>
      <p>SilicoEco 接住意图，把它变成可执行、可判断、可沉淀的工作。人、Agent 和工具都在这条闭环中协作。</p>
    </div>
    <div class="loop-stage" data-reveal>
      <div class="loop-progress" aria-hidden="true">
        {steps.map(() => <i></i>)}
      </div>
      <div class="loop-items">
        {steps.map(([name, detail]) => (
          <article class="loop-item">
            <strong>{name}</strong>
            <p>{detail}</p>
          </article>
        ))}
      </div>
    </div>
  </div>
</section>
